apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tfservingcache.fullname" . }}-test-connection"
  labels:
{{ include "tfservingcache.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "tfservingcache.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
